IF(CMAKE_SYSTEM_NAME MATCHES Windows)
        cmake_minimum_required(VERSION 2.8)
ENDIF(CMAKE_SYSTEM_NAME MATCHES Windows)
message("Processing mvstore server ")
project(server)
SET(MVSTORED_TARGET_NAME "server")
add_subdirectory(src)
